interface:
  display_name: 模块化长图文模板
  short_description: 先生成1000×1600模块预览，确认后转成正文自动延长的HTML与CSS模板
  default_prompt: 使用 $build-modular-longform-templates，先依据参考图生成1000×1600模块预览供我确认；确认后再拆成可独立使用、可组合且正文自动增高的
    HTML + CSS 长图文模块，并交付本地素材、组合示例和延展检查结果。
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
